@model YouInvestMe.Models.Idea

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Idea</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>

<div class="mt-4">    
    <div class="form-group d-inline">
        <a asp-action="Edit" class="btn btn-primary" asp-route-id="@Model?.IdeaId">Edit</a>
    </div>
    <div class="d-inline">
        <a class="btn btn-secondary" asp-action="Index">Back to List</a>
    </div>
</div>

